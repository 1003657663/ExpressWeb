#!/usr/local/bin/node
/*
var exec = require('child_process').exec,
    path = require('path'),
	fs = require('fs');

var input = './src/build';
var output = './src/js';

var cmd = 'jsx --watch '+input+' '+output;

console.info(cmd);

exec(cmd,{encoding:'utf-8'},function (error, stdout, stderr) {
	if(error !== null) {
		console.log(error);
		return;
	}
	console.log(stdout);
});*/


//jsx --watch ./src/build ./src/js